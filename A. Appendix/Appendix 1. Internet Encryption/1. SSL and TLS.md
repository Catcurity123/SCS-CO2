#### 1.1 What is SSL/TLS
(+) The internet is many Routers owned by many different ISPs, on either end is `You` and `Who you are speaking to`.
(+) Most common data transferred on the internet is `Websites`, written in `HTML` and transferred with `HTTP`.
![[Pasted image 20240826165924.png]]

(+) SSL/TLS create a secure, protected tunnel (tunnel means data is encrypted from end to end)
(+) Data sent across a wire can be captured by anyone in the middle, the purpose of SSL/TLS is to protect the data in `three` ways:
![[Pasted image 20240826170526.png]]

(+) SSL/TLS ecosystem involves three key players: `Client`, `Server`, `Certificate Authoritt (CA)`


